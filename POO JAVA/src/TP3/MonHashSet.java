package TP3;

import java.util.HashSet;

public class MonHashSet<E> extends HashSet {
    public MonHashSet<E> sousEnsembleAléatoire(int n) {
        MonHashSet<E> monHashSet = new MonHashSet<>();
        // Ne comprend pas ce qu'il veux dire
        return monHashSet;
    }
}
