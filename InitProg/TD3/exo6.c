#include<stdio.h>

#define N 10

int findDico(int tab[], int val) {
    return 0;
}

int main() {
    int tab[N] = {-2, -1, 3, 4, 5, 6, 7, 8, 9, 10};
    int res = findDico(tab, -1);
    printf("Le résultat est de %d\n", res);
    return 0;
}
